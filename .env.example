# Authentication — pick ONE:
# Option 1: OAuth token from Max/Pro subscription (run `claude setup-token` to get it)
CLAUDE_CODE_OAUTH_TOKEN=
# Option 2: API key (pay-per-token billing)
ANTHROPIC_API_KEY=
LINEAR_API_KEY=
GITHUB_TOKEN=
GITHUB_REPO=owner/repo-name
POLL_INTERVAL_MS=120000
MAX_CONCURRENT=1

# Linear workflow state names (optional — override if your workspace uses different names)
STATUS_BACKLOG=Backlog
STATUS_IN_PROGRESS=In Progress
STATUS_IN_DEVELOPMENT=In Development
STATUS_IN_REVIEW=In Review
STATUS_DONE=Done
STATUS_WAITING=Waiting

# Langfuse tracing (optional)
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_BASE_URL=
