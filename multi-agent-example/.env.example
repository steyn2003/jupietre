# Clawup Secrets — copy to .env and fill in values
# See clawup.yaml 'secrets' section for which keys map where

# ── Required ─────────────────────────────────
TAILSCALE_AUTH_KEY=
TAILNET_DNS_NAME=
TAILSCALE_API_KEY=
HCLOUD_TOKEN=
BRAVE_API_KEY=
ANTHROPIC_API_KEY=

# ── Agent: Juno (pm) ──────────────────────
PM_SLACK_BOT_TOKEN=
PM_SLACK_APP_TOKEN=
PM_LINEAR_API_KEY=
PM_LINEAR_WEBHOOK_SECRET=
PM_GITHUB_TOKEN=
# PM_LINEAR_USER_UUID=  # auto-resolved by `clawup setup`

# ── Agent: Titus (eng) ──────────────────────
ENG_SLACK_BOT_TOKEN=
ENG_SLACK_APP_TOKEN=
ENG_LINEAR_API_KEY=
ENG_LINEAR_WEBHOOK_SECRET=
ENG_GITHUB_TOKEN=
# ENG_LINEAR_USER_UUID=  # auto-resolved by `clawup setup`

# ── Agent: Scout (tester) ──────────────────────
TESTER_SLACK_BOT_TOKEN=
TESTER_SLACK_APP_TOKEN=
TESTER_LINEAR_API_KEY=
TESTER_LINEAR_WEBHOOK_SECRET=
TESTER_GITHUB_TOKEN=
# TESTER_LINEAR_USER_UUID=  # auto-resolved by `clawup setup`
